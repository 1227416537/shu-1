# 书伴 · 本地部署指南（无服务器版）

本项目是**纯前端 PWA**：不需要云服务器、不需要后端，所有数据都保存在浏览器本地。
本机已做"任意位置部署"适配：构建产物使用相对路径，放到任何目录/子路径都能直接跑；
service worker 已启用真实离线缓存，手机安装后断网也能读。

## 一、PC 端

1. 双击项目根目录的 `start.bat`（首次会自动安装依赖并构建）。
2. 浏览器自动打开 `http://localhost:4173/`。
3. 也可以直接把 `dist` 文件夹丢给任意静态服务器（nginx / python http.server / 网盘自取）。

## 二、手机端（同一 WiFi 下访问 PC）

1. 确认手机和 PC 连同一个 WiFi。
2. 查看 PC 局域网 IP（Windows：`ipconfig` 找 IPv4 地址，例如 192.168.28.101）。
3. 手机浏览器打开 `http://<PC的IP>:4173/`。
4. 如果打不开：Windows 防火墙放行 4173 端口（入站规则允许 Node.js / 专用网络）。

## 三、安装成手机 App（PWA，可离线）

- Android Chrome / 国内浏览器：菜单 → 「添加到主屏幕」。
- iOS Safari：分享 → 「添加到主屏幕」。
- 首次打开后 service worker 会自动缓存核心资源，之后断网也能打开已缓存的书。

## 四、手机 ↔ PC 数据同步（没有服务器也能搬家）

App 内「设置 → 存储管理」自带完整存档导出/导入（包含书籍、阅读进度、聊天记录、笔记、人设、设置）：

- PC → 手机：PC 上「导出存档」得到 .json 文件，用微信/网盘/USB 传到手机，手机上「导入存档」。
- 手机 → PC：反向操作即可。
- 建议：读完一段时间就导出一份，防止浏览器数据被清理。

## 五、AI 接口配置

「设置 → API 配置」填入任意 OpenAI 兼容接口（DeepSeek / OpenAI / Gemini / Claude / 自定义）。
数据只从浏览器直连对应 API，不经过任何中转服务器。
想完全离线可用本地模型：在 PC 上跑 Ollama，地址填 `http://<PC-IP>:11434/v1`，手机同 WiFi 可直连。

## 六、（可选）发布到公网

无需服务器时也可以免费发布：把本项目推到 GitHub，用仓库自带的
`.github/workflows/deploy.yml` 自动部署到 GitHub Pages（项目已适配相对路径）。
